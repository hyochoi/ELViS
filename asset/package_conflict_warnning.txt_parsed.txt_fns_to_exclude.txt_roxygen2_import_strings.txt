#' @rawNamespace import(zoo, except=c(index,yearmon,yearqtr))
#' @rawNamespace import(magrittr, except=c(subtract))
#' @rawNamespace import(circlize, except=c(degree))
#' @rawNamespace import(IRanges, except=c(collapse,desc,intersect,median,quantile,sd,setdiff,shift,slice,trim,union))
#' @rawNamespace import(GenomicRanges, except=c(subtract))
#' @rawNamespace import(data.table, except=c(between,first,last,shift,yearmon,yearqtr))
#' @rawNamespace import(igraph, except=c(as_data_frame,degree,groups,union))
#' @rawNamespace import(dplyr, except=c(as_data_frame,between,collapse,desc,first,groups,intersect,last,setdiff,union))
#' @rawNamespace import(Rsamtools, except=c(index))
#' @rawNamespace import(stats, except=c(sd))
#' @rawNamespace import(glue, except=c(trim))
